# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Original high-res source images (not committed)
images-src/

# Node
node_modules/

# macOS
.DS_Store

# Claude Code local state (worktrees, etc.) — never commit
.claude/
